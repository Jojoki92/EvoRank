# EvoRank X5.3 / x5.3-r1

Continue in the existing checkout. Next delivered release: X5.4. All prior data, ranking and delivery rules in AGENTS.md remain.

- `sports-profile-x5.3.js` renders interactive track/wheel/pool SVGs from recorded tempo, distance and consistency components. It extends the existing app, bodygraph assets are untouched. `endurance-ranking-x4.8.js` exposes tempo/distance component scores alongside the unchanged overall score; `triathlon-v9.7.js` passes the same private sport thresholds to that API. The 0–99 consistency bonus is scaled to 0–800 only for area colors, with explicit disclosure that eight active days is regularity, not athletic elite performance. Empty areas are gray; future records and ineligible/Strava records stay excluded.
- Home active sport synchronizes the retained v10.8 rank display state, removing the stale Gym rank. Endurance and Gym use the same five rank tabs. Sport tracking uses the existing triathlon page and timer/GPS/pool controls, not an invented start modal. Rank explanations and their original sport-specific sources are accessible in details rather than exposed above every screen.
- New friends sport values extend the existing authenticated snapshot publication. Do not publish extra raw activities or private profile fields. Older snapshots without sport values show as unavailable rather than borrowing Gym ranks. This delivery does not verify the real backend acceptance/storage of the extra JSON fields.
- X5.1 home/profile toolbars removed; grouped profile links retain plans and progress access. Home settings have switches and one scroll owner. ForgeDrop uses the actual dynamic viewport height. Rank badges retain fresh SVG IDs; the luminance mask now suppresses the supplied artwork's dark rectangle/noise, mirrored in the asset generator.
- CSS remains in the final loaded `evorank-x5.1-ui.css`. Selection never replaces area colors. Keyboard activation keeps focus. SVG gradients, filter and clip IDs remain unique per render. Reduced motion and existing color/density settings persist.
- Tests: `npm.cmd run test:x5` includes X5.3 plus real init/reopen coverage. Build: `.\node_modules\.bin\vinext.cmd build`; HTTP check: `node scripts/smoke-release.mjs`. Browser inspection uses synthetic data and does not read the user's browser profile.
- Release: `npm.cmd run release:prepare`, then `npm.cmd run release:save`. Extracted WINDOWS + NETLIFY in `FREED\x\x5\X5.3`; no ZIP, no additional source copy, no publication. Windows contains only its current guide.
- User's private audio/video and temporary analysis tools stay in ignored `.sites-runtime` or at their original paths; never include these in delivery or Git. Approved generated mock remains in `output/designs/evorank-kompetenzfarben-entwurf.png` for visual comparison.
